Add three Text Input controls
1. Name
2. Email
3. Phone

Add a button --> onselect add below code
UpdateContext({validationform:true});
If(IsBlank(txt_name.Text), UpdateContext({validationform:false}); Set(strcolorn,Color.Red),Set(strcolorn,Color.Black)
);
If(IsBlank(txt_email.Text), UpdateContext({validationform:false}); Set(strcolore,Color.Red),Set(strcolore,Color.Black)
);
If(IsBlank(txt_phone.Text), UpdateContext({validationform:false}); Set(strcolorp,Color.Red),Set(strcolorp,Color.Black)
);

If(validationform, 
Notify("Sucess",NotificationType.Success),
Notify("Please enter all columns",NotificationType.Error));

Go to the controls and update BorderColor
BorderColor for TextInputs based on validation
strcolorn - txt_name
strcolore - txt_email
strcolorp - txt_phone

Add one label and add "Please enter proper Phone" and visible property
If(
    IsMatch(
    txt_phone.Text,
        "^[\\(]{0,1}([0-9]){3}[\\)]{0,1}[ ]?([^0-1]){1}([0-9]){2}[ ]?[-]?[ ]?([0-9]){4}[ ]*((x){0,1}([0-9]){1,5}){0,1}$"
    ),
    false,
    true
)

Add one label and add "Please enter proper Email" and visible property
If(IsMatch(txt_email.Text,"^[A-Za-z0-9](([_\.\-]?[a-zA-Z0-9]+)*)@([A-Za-z0-9]+)(([\.\-]?[a-zA-Z0-9]+)*)\.([A-Za-z]{2,})$"),false,true)

IsBlank(txt_name.Text)


Without RegEx
Add one label and add "Please enter proper Email" and visible property
If(IsMatch(txt_email.Text,Match.Email),false,true)

While blank hide the message
For Email
If(
    IsBlank(txt_email.Text),
    false, If(IsMatch(
        txt_email.Text,
        Match.Email
    ),
    false,
    true)
   
)

for Phone
If(
    IsBlank(txt_phone.Text),false,If(
    IsMatch(
        txt_phone.Text,
        "^[\\(]{0,1}([0-9]){3}[\\)]{0,1}[ ]?([^0-1]){1}([0-9]){2}[ ]?[-]?[ ]?([0-9]){4}[ ]*((x){0,1}([0-9]){1,5}){0,1}$"
    ),
    false,
    true
))



onChange
If(IsBlank(txt_name.Text),Set({strcolorn: Red}),Set({strcolorn: Black}))
If(IsBlank(txt_email.Text),Set({strcolore: Red}),Set({strcolore: Black}))
If(IsBlank(txt_phone.Text),Set({strcolorp: Red}),Set({strcolorp: Black}))
